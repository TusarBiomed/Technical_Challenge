# Technical_Challenge

The task1.R file has to be run at first while having the both datasets in the work environment. The merged dataset generated by this file has been used for subsequent analysis.
The required packages for analysis are included in respective R-files. The packages have to load first before running the code.
Some tasks are done in R-markdown for creating the assigned documentation.
